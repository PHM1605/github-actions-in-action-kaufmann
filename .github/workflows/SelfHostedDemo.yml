name: Self Hosted Demo
on:
  workflow_dispatch:

jobs:
  demo:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - run: echo "Hello"
      - uses: actions/setup-node@v4
        with: 
          node-version: 20
      - run: node --version
